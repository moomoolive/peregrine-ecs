import {expect, describe, it} from "@jest/globals"


describe("getting and setting generation count", () => {
    it("generation count can be extracted from meta", () => {
        expect(true).toBe(true)
    })
})