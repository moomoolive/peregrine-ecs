/**
 * Returns a Uint8Array that is
 * backed by a SharedArrayBuffer
 *
 * @param {number} capacity number of elements
 * array can carry
 * @returns {Uint8Array}
 */
export declare function createSharedUint8Array(capacity: number): Uint8Array;
/**
 * Returns a Int32Array that is
 * backed by a SharedArrayBuffer
 *
 * @param {number} capacity number of elements
 * array can carry
 * @returns {Int32Array}
 */
export declare function createSharedInt32Array(capacity: number): Int32Array;
/**
 * Returns a Float64Array that is
 * backed by a SharedArrayBuffer
 *
 * @param {number} capacity number of elements
 * array can carry
 * @returns {Float64Array}
 */
export declare function createSharedFloat64Array(capacity: number): Float64Array;
//# sourceMappingURL=sharedArrays.d.ts.map